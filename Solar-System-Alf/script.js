// Import the main module to start the Solar System simulation
import { init } from './js/main.js';

// Initialize the application when the page loads
document.addEventListener('DOMContentLoaded', () => {
    init();
});